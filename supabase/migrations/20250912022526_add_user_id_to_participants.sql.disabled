-- Add a user_id column to the participants table to link them to auth.users
ALTER TABLE public.participants
ADD COLUMN user_id UUID IF NOT EXISTS,
ADD CONSTRAINT fk_participants_user_id
FOREIGN KEY (user_id)
REFERENCES auth.users(id)
ON DELETE SET NULL;