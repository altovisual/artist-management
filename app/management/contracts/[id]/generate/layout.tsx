// Este archivo se puede eliminar - no es necesario
// Usar el layout de management que incluye los tabs
export default function GenerateContractLayout({ children }: { children: React.ReactNode }) {
  return children;
}
